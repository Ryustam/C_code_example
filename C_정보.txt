#pragma warning(disable:4996)
scanf 사용가능하게 하는 구문

break문은 자신을 감싸는 가장 가까운 위치의 반복문 하나를 빠져나감
break문은 자신을 감싸는 가장 가까운 위치의 반복문 하나를 빠져나감
break문은 자신을 감싸는 가장 가까운 위치의 반복문 하나를 빠져나감


continue; <- 사용법 숙지

if -> switch 코드 깊이가 줄어든다.



함수에서 return 반환값은 하나!

전역변수는 기본 0으로 초기화됨

지역변수, 전역변수, static

char 배열
char str[]="Good Morning!";


문자열의 끝에는 '\0'이라는 특수문자가 자동으로 삽입되어 실제 문자열길이+1

// "%[^\n]s" \n 오기전까지 모든 문자를 받겠다

scanf("%s",str) - 문자열에는 &를 입력하지 않는다.

배열 끝에 \0 이 있으면 문자열로 인식한다. 없으면 문자 배열


